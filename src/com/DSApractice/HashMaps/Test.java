//Iterative Method
/*      while(depth>0){
        for(int i=0;i<depth;i++){   //Depth or K or for Vertical access
        if(fetchfile(i)==scaler.txt){
        Print Found at i;
        break;}
        else{
        for(int j=0j<i;j++){  ///For the files... or for Horizontal access
        fetchDir(j){
        if(fetchfile(j)==scaler.txt);
        print found at(i,j);
        break;}}
        else{
        Print Not found
        }
        depth--;}


//Recursive Method

folder=home initially

fetchfiles(folder){
if found - true
else{
folder--
fetch(folder);
}

return not found;*/